{
  "name": "degram",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@ton/core": "^0.56.0",
    "@ton/crypto": "^3.2.0",
    "@ton/ton": "^13.11.2",
    "pg": "^8.13.1",
    "tweetnacl": "^1.0.3"
  },
  "scripts": {
    "dev": "node backend/server.js",
    "migrate": "node backend/migrate-cli.js",
    "build": "node backend/migrate-cli.js && npm run build --prefix frontend",
    "vercel-build": "npm run build"
  }
}
